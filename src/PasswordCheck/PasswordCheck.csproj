<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <Nullable>enable</Nullable>
    <TargetFramework>netstandard2.1</TargetFramework>
    <Authors>Sam Curry</Authors>
    <Company />
    <Product />
    <Description>BreachedPasswordService uses the HIBP range search to find how many times a password has appeared in a known breach.</Description>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Http" Version="3.1.0" />
  </ItemGroup>

</Project>
